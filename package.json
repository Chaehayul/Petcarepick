{
  "name": "petcarepick",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "python -m http.server 4173 --directory frontend",
    "dev:backend": "npm --workspace backend run dev",
    "check": "node --check frontend/src/app.js && npm --workspace backend run check",
    "test": "npm --workspace backend test"
  }
}
